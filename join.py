# Aqui vamos juntandando as colunas das diferentes tabelas numa só que vai ser usada para predict
# Nao esquecer de mudar as strings para ints
# Usar o nº de transações por conta
# Usar idade das pessoas
# Remover colunas desnecessárias

import os
import numpy as np
import pandas as pd
import sqlite3

def loan_train_to_db:
    # os.chdir('data')
    # heart = pd.read_csv('loan_train.csv', sep=';', header=0)
    # df = pd.DataFrame(heart, columns= ['loan_id', 'account_id', 'date', 'amount', 'duration', 'payments', 'status'])
    # conn = sqlite3.connect('train.db')
    # c = conn.cursor()
    # ...

def loan_test_to_db:
    # ...


# loan_train_to_db()
# loan_test_to_db()